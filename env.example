# =============================================================================
# Required secrets
# =============================================================================
DEEPGRAM_API_KEY=
OPENROUTER_API_KEY=

SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# =============================================================================
# Optional deployment config
# =============================================================================
# SUPABASE_BUCKET=bucket-name
# SUPABASE_AUTH_MODE=local  # use local JWT validation (default: remote)
# SUPABASE_JWT_SECRET=      # required if SUPABASE_AUTH_MODE=local
# SUPABASE_JWT_AUDIENCE=authenticated
# CORS_ALLOW_ORIGINS=https://your-frontend.com
# RATE_LIMIT=60  # requests per minute, 0 = disabled
